# 네 정밀도 비교와 선택적·적응 정밀도

## 현재 상태

- 후속 요청: Pure FP64 기존 풀이를 추가해 hi/lo 원본·Pure FP64·FP64 보정의 세 경로로 확장했다. 새 두 경로×세 메시를 같은 진단/저장 조건에서 순차 실행한다. 두 풀이×세 메시 각각2프레임 GPU·독립 검산·자동 비교 통과, CPU/중단8개 검사 통과. 이전 동결본 보존, 본 실행 미시작.

- 확인일: 2026-09-13. 굽힘1/100 세 메시·4초의 FP64 보정 실행과 기존 원본 자동 비교를 별도 runtime으로 준비했다. 본 실행은 미시작.
- 원본4초 목표 실행은 기하 검사로 조기 중단됐다. 같은 정상 저장 prefix의 프레임 시간·hi/lo 궤적·독립 검산을 비교한다.
- 본 계산의 plain FP64와 독립 검산의 원래 hi/lo 모듈을 분리하고 조건부 graph 수명을 검증했다. 추가 계측은 명시적 실험 plan에만 연결한다.
- 정밀도 diagnostic의 기존 승인 범위에서 유한 초과를 기록하며 진행한다. 기본 strict 정책·teacher 적격 기준은 유지한다.
- CPU5개·중단3개, 세 메시 각각2프레임과 최종 load 연결 손수건2프레임의 실제 GPU 검증 통과.
  [설정·실행·검증 근거](../../experiments/R1_teacher_velocity_reset/timestep_search/precision_compare/refine64_4s.md).
- 다음은 준비된4초 실행 후 공통 구간 성능·전체 새 구간의 안정성 판정이다. code/experiments 관련 변경만 남겼고 commit·push 없음.

## 앞선 적응 정밀도 검증

- 확인일: 2026-09-13. 후속 적응 FP32/FP64 보조 풀이 구현·순역방향10프레임 비교·별도 내부 계측 완료.
- FP64 상태·힘·접선·참 잔차를 유지하고 FP32 보조 풀이를 시도한다. 정체/상한 초과 시 FP64 GMRES로 전환하며 실패 세대의 재시도를 생략한다.
- 같은 알고리즘의 FP64 대조군을 두어 정밀도 효과를 분리했다. 혼합 가속은 미확인, FP64 보정은 유망한 별도 후보다. 기본 채택은 하지 않았다.
- [실행·수치·판정](../../experiments/R1_teacher_velocity_reset/timestep_search/precision_compare/adaptive_report.md).
- 새 판정/산술5개와 기존13개 검사, 실제 GPU 분기6조건·host copy 거부 검증 통과.
  순방향3경로 각각640단계 독립 GPU 물리·기하 검산도 통과했다. 장기·다른 메시·재개 검증은 미완료다.
- 생성 중 조건부 body 주소를 추적해 기존 자식 graph 검사와 함께 감사했다. GPU 전략 객체와 행렬 세대별 factor 유효성을 유지한다.
- 다음은 FP64 보정 후보의 긴 구간/다른 메시 검증이다. 새 대규모 계산·학습 발행은 미실행.
- code/experiments 관련 worktree만 변경했고 commit·push 없음. 아래는 앞선 FP32 보정의 보존 상태다.

- 확인일: 2026-09-13. FP64/hi-lo ↔ FP32/hi-lo, FP64 ↔ FP32의 진단 실행과 후속 FP32 식 재작성·선택적 보정을 구현했다.
- 사용자 승인으로 변형률 식부터 변경한 뒤 동일 상태의 잔여 오차를 근거로 법선, G/H 계수, 변형률 내적에 FP32 쌍 보정을 추가했다.
- 기본 솔버·FP64 기준·물성·바람·dt·허용오차는 유지하고 별도 동결 runtime에만 적용한다. GPU FP64 연산을 새로 섞지 않았다.
- [실행·상세 수치·한계](../../experiments/R1_teacher_velocity_reset/timestep_search/precision_compare/stable_strain_report.md)가 결과의 소유 문서다.
- CPU/실제 Warp CPU 검사15개, 동일 상태 GPU 힘/에너지/HVP 및 유한 회전 입력 대조를 수행했다. 식 재작성과 최종 보정은 네 경로 모두10프레임/640단계 완료했다.
- 힘 잔차는 개선됐으나 반복·행렬 경고, 에너지·궤적 차이가 남는다. 엄격 teacher 기준·가속·장기 안정성 채택은 미완료다.
- 상태 단일 FP32 경로는 일부 기하 보정 후에도 상태 양자화에 의한 차이가 크다. low를 보관하는 경로와 구분한다.
- 다음은 남은 선형/행렬 오차와 궤적·에너지 차이 검토다. 허용오차 완화·FP64 잔차 보정·물리 모델 변경은 수행하지 않았다.
- 관련 구현·검사·문서만 변경했다. commit·push 없음.
